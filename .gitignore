# Python bytecode
__pycache__/
*.py[cod]

# Large solver artifacts (regenerate with run_main_solve.py)
# GitHub rejects any single file over 100 MB
*.pkl

# Virtual environment
.venv/
venv/

# Test cache
.pytest_cache/

# Generated benchmark training set (80 MB, regenerates deterministically
# from solution_default.pkl via `python generate.py`, seed 20260912)
data/teenpattibench_train.jsonl

# Phase 4 run artifacts (model predictions, eval subsets, scored reports).
# Model predictions and scored reports; outputs, not sources.
runs/
